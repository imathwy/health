interface:
  display_name: "每日饮食照片流水线"
  short_description: "照片饮食分析、来源追踪与 7/30 天本地汇总"
  default_prompt: "Use $daily-diet-pipeline to analyze yesterday’s food photos, record source-aware nutrition ranges, sync the local SQLite index, and generate verified Markdown and static HTML reports."
policy:
  allow_implicit_invocation: true
