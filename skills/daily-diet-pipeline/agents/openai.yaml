interface:
  display_name: "每日饮食照片流水线"
  short_description: "照片饮食、健康指标与 7/30 天本地趋势"
  default_prompt: "Use $daily-diet-pipeline to inspect every photo from yesterday, screen food-related assets before reconstructing meals, preserve source-aware nutrition and health observations with explicit missingness, sync the local SQLite index, and generate verified daily and longitudinal reports."
policy:
  allow_implicit_invocation: true
