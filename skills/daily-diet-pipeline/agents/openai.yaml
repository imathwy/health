interface:
  display_name: "每日饮食照片流水线"
  short_description: "个人档案、照片饮食、健康指标与本地趋势"
  default_prompt: "Use $daily-diet-pipeline to read the active private personal profile and medical index, inspect every photo from yesterday, screen food-related assets before reconstructing meals, preserve source-aware nutrition and health observations with explicit missingness, sync the local SQLite index, and generate verified local reports."
policy:
  allow_implicit_invocation: true
